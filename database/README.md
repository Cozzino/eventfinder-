# Database

Database assets for EventFinder.

## M1 scope

The `schema/` directory contains the PostgreSQL + PostGIS schema for EventFinder Core. Migrations and backups are kept in separate folders for future operational work.
